// const databaseConnection = require("sqlite3")

module.exports = {
    searchDatabase: () => {
        // talk to database
        // const result = databaseConnection("SELECT * ")
        // return result from database
        return { "name": "waterbottle"}
    }
}